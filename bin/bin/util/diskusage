#!/usr/bin/env sh
# desc: show top 20 entries by disk usage in a directory

if [ "$#" -eq 0 ]; then
    DIR=`pwd`
elif [ "$#" -eq 1 ]; then
    DIR=$1
else 
    echo "Usage: $0 <path>"
    exit 1
fi

du -h --max-depth=1 $DIR 2> /dev/null | sort -rh | head -n20
